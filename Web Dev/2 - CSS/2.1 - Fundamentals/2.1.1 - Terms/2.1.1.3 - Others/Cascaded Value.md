---
tags: 
 - css
 - term
 - fundamental
---

- The browser follows these steps to resolve every property for every element on the page. A declaration that “wins” the cascade is called a **cascaded value**.
- There is at most one cascaded value per property per element.

> A cascaded value is a value for a particular property applied to an element as a result of the cascade.
